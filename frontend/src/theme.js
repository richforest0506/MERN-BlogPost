import { createMuiTheme} from "@material-ui/core/styles";

const alteredTheme = createMuiTheme({
    palette: {
      primary: {
        main: "#1e3d59"
      },
      secondary: {
        main: "#ff6e40"
      }
    }
  }); 
export default alteredTheme;